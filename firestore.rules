rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /jukenbansoSpaces/{spaceId} {
      allow get, create, update: if request.auth != null;
      allow list, delete: if false;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
